using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterSlot : MonoBehaviour
{
    /*
    public CharacterObj currentCharacter;

    public void AssignCharacter(CharacterObj assignedCharacter){
        currentCharacter = assignedCharacter;
        assignedCharacter.slot = this;
        assignedCharacter.transform.parent = transform;
        assignedCharacter.rt.anchoredPosition = Vector3.zero;
    }

    public Character GetCharacter(){
        if (currentCharacter == null){
            return null;
        }
        return currentCharacter.GetCharacter();
    }
    */
}
